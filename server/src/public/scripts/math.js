
/** @description insert newNde after refenceNode
  * @param {Node} newNode
  * @param {Node} referenceNode
 */

/**
 *
 * @returns {{b: number, c: number}}
 */
export default function sum() {
    const { a, ...rest } = { a: 1, b: 2, c: 3 };
    const ret = rest;
    console.log(ret);
    return ret;
}
